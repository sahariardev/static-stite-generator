export default function ConfigLayout({
                                       children,
                                   }: {
    children: React.ReactNode
}) {
    return (
        <div>
            {children}
        </div>
    )
}
